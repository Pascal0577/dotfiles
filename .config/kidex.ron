Config(
  ignored: ["*.tmp", "*.bak"], // A list of patterns to be ignored in all directories
  directories: [
    WatchDir(
      path: "/home/pascal/", // The root folder to be indexed
      recurse: true, // Recursively index and watch all subfolders
      ignored: [".cache", "icons", "*.svg"], // Ignore patterns specifically for this directory
    ),
  ],
)
